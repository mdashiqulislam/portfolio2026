import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Keeps the dev overlay badge from sitting on top of the hero's logo rail.
  devIndicators: false,
};

export default nextConfig;
